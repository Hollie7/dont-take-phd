# Server-side API keys (never add VITE_ prefix — these must NOT be exposed to the browser)
# Set these in Vercel Dashboard > Settings > Environment Variables for production.
# For local dev, copy this file to .env and fill in your keys.

DEEPSEEK_API_KEY=your_deepseek_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
